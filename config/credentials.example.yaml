# FPL Credentials - COPY TO credentials.yaml AND FILL IN
# DO NOT COMMIT credentials.yaml TO VERSION CONTROL

# FPL Login (required for authenticated endpoints)
fpl:
  email: "your-fpl-email@example.com"
  password: "your-fpl-password"
